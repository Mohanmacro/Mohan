"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const constants_1 = require("./constants");
function trytesToTrits(trytes) {
    var i, tryteTrits;
    const size = trytes.length;
    const trits = new Array(size * 3);
    for (i = 0; i < size; ++i) {
        tryteTrits = constants_1.TRYTES_TO_TRITS[trytes[i]];
        trits[i * 3 + 0] = tryteTrits[0];
        trits[i * 3 + 1] = tryteTrits[1];
        trits[i * 3 + 2] = tryteTrits[2];
    }
    return trits;
}
exports.trytesToTrits = trytesToTrits;
